- if @user.errors.any?
  #error_explanation
    .alert.alert-danger
      |  Форма содержит ошибки:
      / =pluralize(@user.errors.count, "ошибка")
    ul
      - @user.errors.full_messages.each do |msg|
        li
          = msg
